{
  "name": "tarahan-choob-website",
  "version": "1.0.0",
  "private": true,
  "description": "Tarahan Choob website — static frontend with Vercel serverless contact API. Use npm run dev for local Express server.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "vercel-build": "node -e \"console.log('Static site — no build step required')\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "nodemailer": "^6.9.14",
    "validator": "^13.12.0"
  }
}
